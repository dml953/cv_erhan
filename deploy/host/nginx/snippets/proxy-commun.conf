# En-têtes transmis aux conteneurs.
# Sans X-Forwarded-Proto, Dotclear et WordPress construisent leurs URL en
# http:// alors que le visiteur est en https:// : les feuilles de style sont
# alors bloquées par le navigateur (contenu mixte) et les redirections bouclent.

proxy_http_version 1.1;
proxy_set_header Connection         "";

proxy_set_header Host               $host;
proxy_set_header X-Real-IP          $remote_addr;
proxy_set_header X-Forwarded-For    $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto  $scheme;
proxy_set_header X-Forwarded-Host   $host;
proxy_set_header X-Forwarded-Port   $server_port;

proxy_connect_timeout 5s;
proxy_send_timeout    60s;
proxy_read_timeout    60s;

proxy_buffering on;
proxy_buffer_size       8k;
proxy_buffers        16 8k;
